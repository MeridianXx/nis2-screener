// Source of truth: PRD section 6 ("Datamodell"). ExplanationCache is
// active in session 2 and used by /api/explain. CompanyCache and
// AssessmentLog are introduced together with Roaring/logging in session 3.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DATABASE_URL_UNPOOLED")
}

model CompanyCache {
  orgnr     String   @id
  name      String
  city      String?
  sniCode   String
  sniLabel  String?
  employees Int?
  turnover  Float?
  balance   Float?
  fetchedAt DateTime @default(now())
  expiresAt DateTime
}

model AssessmentLog {
  id        String   @id @default(cuid())
  orgnr     String?
  verdict   String
  sector    String?
  sizeClass String?
  createdAt DateTime @default(now())
}

model ExplanationCache {
  cacheKey  String   @id
  text      String
  createdAt DateTime @default(now())
  expiresAt DateTime
}

model SearchCache {
  queryHash String   @id
  results   Json
  createdAt DateTime @default(now())
  expiresAt DateTime
}
