// TODO(session 3): Implement Roaring API client.
// - Read ROARING_API_KEY and ROARING_BASE_URL from env (server-side only).
// - Expose searchCompanies(query: string) and getCompanyProfile(orgnr: string).
// - Translate Roaring errors into user-facing Swedish messages.
// - Add 60-day cache via lib/cache.ts.
export {};
