// TODO(session 3): Implement Postgres-backed caching helpers.
// - getCompanyCache(orgnr) / setCompanyCache(profile) with 60-day TTL.
// - getExplanationCache(key) / setExplanationCache(key, text) with 30-day TTL.
// - All caching must be explicit and visible at call sites — no implicit layers.
export {};
